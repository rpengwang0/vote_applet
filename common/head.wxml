<template name="head">
  <view class="page-head">
    <view class="page-head-title">1{{title}}</view>
    <view class="page-head-line"></view>
    <view wx:if="{{true}}" class="page-head-desc">1{{desc}}</view>
  </view>
</template>
